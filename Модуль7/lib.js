
/*window.lib = (function () {



function UserList() {
      this.users = [];
      this.add = function(user) {
          this.users.push(user);
      };
      this.getAllUsers = function() {
          console.log(this.users);
      }
  }

  var allUsers = new UserList();*/

    /*var start = function() {
        var fio = prompt('ввести имя и фамилию пользователя для регистрации');

        if (fio !== null) {
            var date = new Date();
            var arr = fio.split(' ');
            var regDate = date.getHours() + ':' + date.getMinutes() + ':' + date.getSeconds();
            var newUser = new user(arr[0],arr[1],regDate);
            allUsers.add(newUser);
            start();
        } else {
            allUsers.getAllUsers();
        }
    };


    return {
        start
    };

})();
*/






